<!-- Jeux-esprit.UI/Views/AddPlayerWindow.xaml -->
<Window x:Class="JeuxEspritUI.Views.AddPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JeuxEspritUI.Views"
        mc:Ignorable="d"
        Title="Ajouter un joueur" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#f0f0f0"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0" 
                   Text="Ajouter un nouveau joueur" 
                   FontSize="22" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,20" 
                   HorizontalAlignment="Center"/>

        <!-- Formulaire -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,0">
                <!-- Nom -->
                <TextBlock Text="Nom:" FontSize="14" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtNom" Height="35" FontSize="14" Padding="5"/>

                <!-- Email -->
                <TextBlock Text="Email:" FontSize="14" Margin="0,15,0,5"/>
                <TextBox x:Name="TxtEmail" Height="35" FontSize="14" Padding="5"/>

                <!-- Avatar -->
                <TextBlock Text="Avatar (description ou chemin):" FontSize="14" Margin="0,15,0,5"/>
                <TextBox x:Name="TxtAvatar" Height="35" FontSize="14" Padding="5" Text="default_avatar.png"/>

                <!-- Type de joueur -->
                <TextBlock Text="Type de joueur:" FontSize="14" Margin="0,15,0,5"/>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <RadioButton x:Name="RadioAmateur" Content="Amateur" GroupName="UserType" Margin="0,0,20,0" IsChecked="True" Checked="RadioAmateur_Checked"/>
                    <RadioButton x:Name="RadioProfessionnel" Content="Professionnel" GroupName="UserType" Checked="RadioProfessionnel_Checked"/>
                </StackPanel>

                <!-- Champs spécifiques Amateur -->
                <Grid x:Name="GridAmateurFields" Margin="0,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Niveau d'expérience (1-5):" FontSize="14" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtAmateurLevel" Grid.Row="1" Height="35" FontSize="14" Padding="5" Text="1"/>
                </Grid>

                <!-- Champs spécifiques Professionnel -->
                <Grid x:Name="GridProFields" Margin="0,15" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Classement initial:" FontSize="14" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtProRanking" Grid.Row="1" Height="35" FontSize="14" Padding="5" Text="0"/>
                    
                    <TextBlock Grid.Row="2" Text="Parties gagnées initiales:" FontSize="14" Margin="0,15,0,5"/>
                    <TextBox x:Name="TxtProWins" Grid.Row="3" Height="35" FontSize="14" Padding="5" Text="0"/>
                </Grid>

                <!-- Message d'erreur -->
                <TextBlock x:Name="TxtError" 
                           Foreground="Red" 
                           FontSize="12" 
                           TextWrapping="Wrap" 
                           Margin="0,15,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Boutons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="BtnCreate" 
                    Content="Créer le joueur" 
                    Width="140" 
                    Height="35" 
                    FontSize="14" 
                    Margin="10,0" 
                    Background="#2ecc71" 
                    Foreground="White"
                    Click="BtnCreate_Click"/>
            
            <Button x:Name="BtnCancel" 
                    Content="Annuler" 
                    Width="120" 
                    Height="35" 
                    FontSize="14" 
                    Margin="10,0" 
                    Background="#e74c3c" 
                    Foreground="White"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>